<%= form_with(model: santri) do |form| %>
  <% if santri.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(santri.errors.count, "error") %> prohibited this santri from being saved:</h2>

      <ul>
        <% santri.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :deskripsi, style: "display: block" %>
    <%= form.text_field :deskripsi %>
  </div>

  <div>
    <%= form.label :image_url, style: "display: block" %>
    <%= form.file_field :image_url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
