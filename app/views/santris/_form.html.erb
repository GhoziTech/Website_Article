<%= form_with(model: santri) do |form| %>
  <% if santri.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(santri.errors.count, "error") %> prohibited this santri from being saved:</h2>

      <ul>
        <% santri.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nama, style: "display: block" %>
    <%= form.text_field :nama %>
  </div>

  <div>
    <%= form.label :kelas, style: "display: block" %>
    <%= form.text_field :kelas %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
